import BaseBusiness = require("./../BaseBusiness");
import IRoomModel = require("./../../model/interfaces/IRoomModel");

interface IRoomBusiness extends BaseBusiness<IRoomModel> {

}
export = IRoomBusiness;